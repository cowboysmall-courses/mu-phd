[project]
name = "mu-phd"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jerry Kiely", email = "jerry@cowboysmall.com" }
]
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.10.9",
]


[project.scripts]
# mu-phd = "mu_phd:main"
adleman = "adleman.experiment:main"
xor = "atam.experiment_xor:main"
counter = "atam.experiment_counter:main"
sdc = "sdc.experiment:main"


[build-system]
requires = ["uv_build>=0.10.12,<0.11.0"]
build-backend = "uv_build"


[tool.uv.build-backend]
namespace = true
