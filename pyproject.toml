[project]
name = "mu-phd"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jerry Kiely", email = "jerry@cowboysmall.com" }
]
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.10.9",
]


[project.scripts]
# mu-phd = "mu_phd:main"
adleman = "adleman.experiment:main"
atam-xor = "atam.experiment_atam_xor:main"
atam-counter = "atam.experiment_atam_counter:main"
sdc-parity = "sdc.experiment_sdc_parity:main"
sdc-add = "sdc.experiment_sdc_add:main"


[build-system]
requires = ["uv_build>=0.10.12,<0.11.0"]
build-backend = "uv_build"


[tool.uv.build-backend]
namespace = true
