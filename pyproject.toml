[project]
name = "mu-phd"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jerry Kiely", email = "jerry@cowboysmall.com" }
]
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.10.9",
]


[project.scripts]
# mu-phd = "mu_phd:main"
adleman = "experiments.adleman.experiment:main"
atam-xor = "experiments.atam.atam_xor:main"
atam-counter = "experiments.atam.atam_counter:main"
sdc-add = "experiments.sdc.sdc_add:main"
sdc-parity = "experiments.sdc.sdc_parity:main"


[build-system]
requires = ["uv_build>=0.10.12,<0.11.0"]
build-backend = "uv_build"


[tool.uv.build-backend]
namespace = true
